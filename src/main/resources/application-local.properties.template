# ===========================================
# LOCAL DEVELOPMENT SECRETS TEMPLATE
#
# Copy this file to `application-local.properties`
# (same folder) and fill in your real local MySQL
# password. That filename is already in .gitignore
# so it will never be committed.
#
# Then run with:
#   mvn spring-boot:run -Dspring-boot.run.profiles=local
# ===========================================

spring.datasource.password=YOUR_LOCAL_MYSQL_PASSWORD_HERE
